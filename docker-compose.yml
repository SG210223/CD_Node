version: '3.9'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: app:latest
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    command: npm start
